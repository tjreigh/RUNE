{
  "name": "rune-web",
  "private": true,
  "type": "module",
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "clean": "node frontend/clean.mjs",
    "build": "yarn clean && tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "yarn build && node --test tests/frontend/frontend_validation.test.js"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
