[project]
name = "rune"
version = "0.2.0"
description = "RUNE (Runtime Unicode Numeric Evaluation), a pure-stdlib Python esolang"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
